import { Placeholder } from '@/components/Placeholder';

export default function ReservationsPage() {
  return (
    <Placeholder
      title="Mis reservas"
      description="Reserva, confirmación (Ocupar), liberación anticipada y comprobante QR (UC3, Fn4)."
    />
  );
}
