import { Placeholder } from '@/components/Placeholder';

export default function HistoryPage() {
  return (
    <Placeholder
      title="Historial de reservas"
      description="Registro inmutable filtrable por usuario y rango de fechas, exportable (Fn3, Pantalla 7)."
    />
  );
}
